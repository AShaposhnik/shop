import { RandomTextColorDirective } from './random-text-color.directive';

describe('RandomTextColorDirective', () => {
  it('should create an instance', () => {
    // const directive = new RandomTextColorDirective();
    // expect(directive).toBeTruthy();
  });
});
